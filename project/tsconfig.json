// @ts-check
// checkJs 속성
{
  "compilerOptions": {
    "allowJs": true,
    "target": "ES5",
    "outDir": "/.built", // ts가 어디로 들어갈 것인지.
    "moduleResolution": "Node"
  },
  "include": ["./src/**/*"] // 어떤 폴더를 대상으로 compile 처리를 할 것인지.
}
